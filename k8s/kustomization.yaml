apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: book-order-demo

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - databases.yaml
  - apps.yaml
  - order-hpa.yaml
